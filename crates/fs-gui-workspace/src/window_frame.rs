//! `WindowFrame` — window chrome data and CSS constants.
//!
//! All rendering is done in `shell.rs` via iced widgets.
//! This file keeps the `FSNOBJ_CSS` constant for future iced theme integration.

//! CSS constants kept for reference — used in iced theme configuration.
pub const FSNOBJ_CSS: &str = "";

/// Stub type kept for backward-compatible imports.
pub struct WindowFrame;

/// Stub type kept for backward-compatible imports.
pub struct MinimizedWindowIcon;
