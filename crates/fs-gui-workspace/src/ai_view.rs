//! AI Manager view stub — placeholder for iced migration.
//!
//! `AiApp` will be implemented as a full iced application in a future phase.
//! For now it serves as a named type so imports in `lib.rs` compile.

//! Stub app state for the AI manager.
pub struct AiApp;
